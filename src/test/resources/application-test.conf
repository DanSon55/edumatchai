database {
    url = "jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1"
    username = "sa"
    password = ""
}

server {
    port = 8081
    host = "localhost"
}

search {
    maxResults = 10
    defaultPageSize = 5
}

api {
    keys {
        googleMaps = "test-key"
        openai = "test-key"
    }
}